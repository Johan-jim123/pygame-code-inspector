# pygame-code-inspector
An automated static code inspector for Pygame projects. Uses Python's ast module to scan scripts for setup errors (pygame.init/quit), missing FPS limiters, unmonitored loops, and unused RAM assets without executing the file—delivering an instant code health report and grade (A to F).
